name: 'Agent Wormhole'
description: 'Detect self-replicating prompt payloads and agent permission overreach in AI agent configs'
author: 'runningoffcode'
branding:
  icon: 'shield'
  color: 'red'

inputs:
  path:
    description: 'Directory to scan'
    required: false
    default: '.'
  fail-on:
    description: 'Exit nonzero at or above this severity: critical, high, medium, low, never'
    required: false
    default: 'high'
  blast-radius:
    description: 'Include blast-radius scoring in the report'
    required: false
    default: 'true'
  local-only:
    description: 'Skip global settings/MCP/skill checks (recommended in CI)'
    required: false
    default: 'true'

runs:
  using: 'composite'
  steps:
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Run Agent Wormhole
      shell: bash
      run: |
        set -o pipefail   # `tee` must not swallow a nonzero scan exit code

        # The scanner lives in the action's own checkout; the tree being
        # scanned is the caller's workspace. Keep those separate, or the
        # action scans itself and passes on everything.
        export PYTHONPATH="${{ github.action_path }}:${PYTHONPATH:-}"
        TARGET="$(cd "$GITHUB_WORKSPACE" && cd "${{ inputs.path }}" && pwd)"

        ARGS="$TARGET --fail-on ${{ inputs.fail-on }} --no-color"
        [ "${{ inputs.local-only }}" = "true" ] && ARGS="$ARGS --local-only"
        [ "${{ inputs.blast-radius }}" = "true" ] && ARGS="$ARGS --blast-radius"

        python3 -m wormhole scan $ARGS 2>&1 | tee "$RUNNER_TEMP/wormhole.txt"
      working-directory: ${{ github.workspace }}

    - name: Summary
      if: always()
      shell: bash
      run: |
        {
          echo "## Agent Wormhole"
          echo '```'
          cat "$RUNNER_TEMP/wormhole.txt" 2>/dev/null || echo "no output"
          echo '```'
        } >> "$GITHUB_STEP_SUMMARY"
