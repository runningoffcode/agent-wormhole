{
  "name": "mcp-trade-guard",
  "version": "0.1.0",
  "description": "A policy gate between an AI agent and an agentic-trading MCP: scans the text the agent reads for injection, and enforces per-order and daily dollar caps as arithmetic the agent cannot argue with.",
  "type": "module",
  "license": "Apache-2.0",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "keywords": ["mcp", "agentic-trading", "prompt-injection", "trading-guard", "ai-agent-security", "robinhood"],
  "peerDependencies": { "wormhole-x402": "^0.3.0" },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5",
    "vitest": "^2",
    "wormhole-x402": "^0.3.0"
  }
}
